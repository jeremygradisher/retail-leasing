
    <h1 style="font-family: 'simplifica';font-size:30pt;width:100%;text-align:center;">Spaces</h1>
    <table class="table table-striped add-responsive /*print-none*/" style="/*display:none;*/">
      <thead>
        <tr>
          <!--<th>Area Image:</th>-->
          <th style="text-align:left;"><b>Suite #:</b></th>
          <th style="text-align:left;"><b>Area SF:</b></th>
          <th style="text-align:left;"><b>TC Status:</b></th>
          <th style="text-align:left;"><b>Deals:</b></th>
          <th style="text-align:left;"><b>Primary Deal:</b></th>
          <th style="text-align:left;"><b>Lease Status:</b></th>
          <th style="text-align:left;"><b>Merch Status:</b></th>
          <th class="print-none" style="text-align:left;"><b>Actions:</b></th>
        </tr>
      </thead>
      <tbody>
        <% @areas.each do |area| %>
          <tr>
            <td style="text-align:left;"><%= area.suite_number %></td>
            <td style="text-align:left;"><%= number_with_delimiter(area.area_sqft, :delimiter => ',') %></td>
            <td style="text-align:left;"><%= area.status %></td>
            <td style="text-align:left;"><%= area.deals.count %></td>
            <td style="text-align:left;"><% if area.deals.count > 0 %><%= area.deals.first.deal_name %><% else %>*No deal associated<% end %></td>
            <td style="text-align:left;"><% if area.deals.count > 0 %><%= area.deals.first.lease_status %><% else %>*No deal associated<% end %></td>
            <td style="text-align:left;"><% if area.deals.count > 0 %><%= area.deals.first.merchandising_status %><% else %>*No deal associated<% end %></td>
     
            <td style="text-align:left;" class="print-none">
              <%= link_to 'View',
                          area_path(area), 
                          :class => 'btn btn-default btn-xs add-responsive-bottom print-none',
                          :style => 'width:60px;margin-bottom:2px;' %>
              <% if current_user.is_admin? || current_user.role == 'Admin' || current_user.role == 'Editor' %>
              <br>
              <%= link_to 'Edit',
                          edit_area_path(area), :class => 'btn btn-primary btn-xs responsive-none print-none',
                          :style => 'width:60px;' %>
              
              <%#= link_to 'Destroy',
                          area_path(area),
                          :method => :delete,
                          :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                          :class => 'btn btn-xs btn-danger responsive-none print-none',
                          :style => 'width:60px;margin-bottom:2px;' %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

