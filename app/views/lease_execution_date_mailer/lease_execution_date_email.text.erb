Cheers <%= @user.first_name %>,

<% @schedules.each do |s_id| %>
<%= Schedule.find(s_id).area.suite_number %> from the project <%= Schedule.find(s_id).area.map.project.name %> has a Lease Execution date coming on <%= @date %>.

<% end %>
Are we ready?