Cheers <%= @user.first_name %>,

<% @schedules.each do |s_id| %>
<%= Schedule.find(s_id).area.name %> from the project <%= Schedule.find(s_id).area.map.project.name %> has a Open For Business date coming on <%= @date %>.

<% end %>
Are we ready?